---
- name: Update homeassistant files and install correct homeassistant version
  hosts: "{{ target_host }}"
  become: yes
  become_user: ansible
  vars_files:
    - vars/config.yaml
    - vars/vault.yaml
  roles:
    - homeassistant
