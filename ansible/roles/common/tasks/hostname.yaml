---
- name: Set hostname
  hostname:
    name: "{{ hostname }}"

- name: Change hostname in /etc/hosts
  copy:
    content: |
      127.0.0.1     localhost
      127.0.1.1     {{ hostname }}
    dest: /etc/hosts
